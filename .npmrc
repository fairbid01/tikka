# Enforce the pinned Node/pnpm versions (see `.nvmrc` and `engines` in
# `package.json`). Fail `pnpm install` when the running Node major does not
# match the declared `engines.node`.
engine-strict=true

# All packages are managed from the repo root via pnpm-workspace.yaml.
